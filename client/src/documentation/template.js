/*
========================================================================
                        JSDoc Template Reference
========================================================================

This file contains ready-to-use JSDoc templates for different types of
functions in your project. Copy the appropriate template and paste it
above your function, then fill in the details.
*/

/* -----------------------------------
   1. API Function Template
----------------------------------- */
/**
 * [Short description of what the API function does]
 *
 * @param {string} workspaceId - The ID of the workspace (example)
 * @param {string} divisionId - The ID of the division (example)
 * @param {Object} [data] - Optional payload for POST/PUT/PATCH requests
 * @returns {Promise<Object>} A promise resolving to the API response
 *
 * @example
 * const result = await getVehicles("workspace1", "divisionA");
 */
export const apiFunctionTemplate = () => {
    // This is just a placeholder; copy the comment above to your real function
};

/* -----------------------------------
   2. Utility Function Template
----------------------------------- */
/**
 * [Short description of what the utility function does]
 *
 * @param {TYPE} paramName - Description of the parameter
 * @param {TYPE} [optionalParamName] - Optional parameter description
 * @returns {TYPE} Description of the return value
 *
 * @example
 * const total = sumNumbers([1, 2, 3]);
 */
export const utilityFunctionTemplate = () => {
    // Placeholder
};

/* -----------------------------------
   3. UI Component Template (React)
----------------------------------- */
/**
 * [Short description of the React component]
 *
 * @param {Object} props - Props object
 * @param {TYPE} props.propName - Description of prop
 * @param {TYPE} [props.optionalProp] - Optional prop
 * @returns {JSX.Element} The rendered component
 *
 * @example
 * <MyButton label="Click me" onClick={handleClick} />
 */
export const uiComponentTemplate = () => {
    // Placeholder
};

/* -----------------------------------
   Usage Instructions
-----------------------------------
1. Copy the appropriate comment block (above the placeholder function).
2. Paste it above your real function.
3. Replace placeholders with real parameter names, types, and descriptions.
4. Add @example blocks if you want to show usage examples.
*/
